<template>
  <!--  -->
  <div class="flex items-center justify-center h-full">
    <div class="bg-black bg-opacity-80 w-full max-w-md lg:h-[80vh] py-4 px-8 rounded-lg">
      <form class="p-8">
        <h3 class="lg:text-[32px] font-medium lg:mb-[28px] text-white">Sign In</h3>

        <div class="mt-16">
          <div class="mb-6">
            <input
              type="text"
              placeholder="Email or phone number"
              class="input input-bordered w-full max-w-full !outline-none !bg-[#333]"
            />
          </div>
          <div class="mb-4">
            <input
              type="text"
              placeholder="Password"
              class="input input-bordered w-full max-w-full !outline-none !bg-[#333]"
            />
          </div>
        </div>
        <button class="btn btn-primary w-full mt-7 !font-semibold">Sign In</button>
      </form>
    </div>
  </div>
</template>

<!-- 

    color: #fff;
    font-size: 32px;
    font-weight: 500;
    margin-bottom: 28px;
 -->
